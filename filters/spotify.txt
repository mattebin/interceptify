# Interceptify — Spotify filter rules
#
# Syntax (one rule per line):
#   example.com               Exact hostname match (any path)
#   */ads/*                   Path substring match (host-agnostic unless a host is on the line)
#   host.com/ads/             Host + path-prefix match
#   re:^https://.*/ads/v\d+   Full-URL regex (Python re syntax), prefix with "re:"
# Lines starting with # and blank lines are ignored.

# --- Spotify ad + telemetry endpoints ---
spclient.wg.spotify.com/ads/
spclient.wg.spotify.com/ad-logic/
spclient.wg.spotify.com/gabo-receiver-service/
spclient.wg.spotify.com/melody/
adeventtracker.spotify.com
analytics.spotify.com

# --- Third-party ad networks sometimes contacted by the Spotify client ---
pagead2.googlesyndication.com
googleads.g.doubleclick.net
